import React from "react";

import { Container } from "styles/components/SecondaryTitle";

function SecondaryTitle({ children }) {
  return <Container>{children}</Container>;
}

export default SecondaryTitle;
